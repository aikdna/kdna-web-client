name: Publication disabled for current candidate
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  publication-boundary:
    runs-on: ubuntu-latest
    steps:
      - name: Candidate publication is not authorized
        run: |
          echo "Current component candidate publication requires a separately approved release workflow."
          exit 1
