{
  "name": "@aikdna/kdna-web-client",
  "version": "0.2.1",
  "description": "Browser-safe KDNA utilities \u2014 file selection, metadata inspection, upload, and load-plan state management. Never performs decryption.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "default": "./src/index.js"
    }
  },
  "types": "./src/index.d.ts",
  "files": [
    "src/",
    "docs/",
    "LICENSE",
    "NOTICE",
    "README.md",
    "SECURITY.md"
  ],
  "scripts": {
    "ci": "npm run validators:check && npm test && npm run typecheck && npm run lint && npm run build && npm run size && npm run naming:check && npm pack --dry-run --json",
    "test": "node --test tests/client.test.js",
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/lint.js",
    "build": "node scripts/build.js",
    "size": "node scripts/size.js",
    "validators:generate": "node scripts/generate-runtime-validators.js --write",
    "validators:check": "node scripts/generate-runtime-validators.js --check",
    "naming:check": "node scripts/check-naming-integrity.js",
    "release:binding": "node scripts/check-release-binding.js",
    "prepublishOnly": "npm run ci"
  },
  "engines": {
    "node": ">=20"
  },
  "peerDependencies": {},
  "dependencies": {},
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "esbuild": "^0.25.6",
    "typescript": "^5.9.3"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aikdna/kdna-web-client.git"
  },
  "bugs": {
    "url": "https://github.com/aikdna/kdna-web-client/issues"
  },
  "homepage": "https://github.com/aikdna/kdna-web-client",
  "keywords": [
    "kdna",
    "kdna-web-client",
    "browser",
    "file-api",
    "load-plan",
    "judgment-asset"
  ]
}
